Windows Pro Install
	US language (+ SR Latin keyboard)
	Serbian Region
	Install Drive - 350GB
	Local Account
	Deny all telemetry
	Install Wifi Driver separately

Updates and Drivers (Manual)
Pause Windows Update 
Vendor app & drivers
	Update Microsoft Store
	Install Vendor App
	Update Drivers automatically
	Update CPU/GPU/Chipset latest drivers
	Update BIOS
Resume Windows Update
	Repeat until no updates left

Enable Powershell (Script oneliner

Activate Windows

Set up drives
	Install Drive - 350GB - Already there
	Dev Drive - Unallocated 150GB
	Data Drive - Rest

Firefox (Winget + manual setup)
	Login to bitwarden web via key
	Login to google web via key
	Login to Mozilla account
	Activate sync
	Login to Bitwarden extension via key

Insync (Manual)
	Turn off all base folder syncs
	Setup manually to D:\data
	Set exclusions

Git & GitHub (Script)
	Installs git via winget
	Creates ssh profile and opens GitHub to paste it in
	overwrites global .gitconfig from gist url
	sets global user.name and user.email
	

Set up Windows Terminal (Script)


-- Settings automation

-- Debloat process

-- Application installation

